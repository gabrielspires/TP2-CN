#include "Edge.hpp"
#include "Input.hpp"
#include <cstdlib>

class Graph{
public:
    Edge **graph;  //matriz de arestas

    Graph(int num_points, Input input); //Construtor
    ~Graph(); //Destrutor

private:
	
};
